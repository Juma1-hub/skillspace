"use client";import {useEffect,useState} from 'react';type Tx={id:number;type:string;amount:number;status:string;date:string;note:string};export default function Transactions(){const [tx,setTx]=useState<Tx[]>([]);useEffect(()=>{setTx(JSON.parse(localStorage.getItem('ss_transactions')||'[]'))},[]);return <div className="page"><div className="hero"><h1>Transactions</h1><p>Your task rewards, access payments and withdrawal requests appear here.</p></div>{tx.length?<table className="table"><thead><tr><th>Date</th><th>Type</th><th>Amount</th><th>Status</th></tr></thead><tbody>{tx.map(x=><tr key={x.id}><td>{x.date}</td><td>{x.type}<br/><small className="muted">{x.note}</small></td><td>{x.amount>=0?'+':''}{x.amount.toFixed(2)} USD</td><td><span className="pill">{x.status}</span></td></tr>)}</tbody></table>:<div className="empty card">No transactions yet. Complete a task to create your first earning transaction.</div>}</div>}
