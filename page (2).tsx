"use client";import {useEffect,useState} from 'react';import {defaultContact} from '../../lib/data';export default function Support(){const [c,setC]=useState(defaultContact);useEffect(()=>{try{setC({...defaultContact,...JSON.parse(localStorage.getItem('ss_contact')||'{}')})}catch{}} ,[]);return <div className="page"><div className="hero"><h1>Support</h1><p>{c.message}</p></div><div className="grid"><div className="card"><h3>Email</h3><p className="muted">{c.email}</p><a className="btn" href={`mailto:${c.email}`}>EMAIL SUPPORT</a></div><div className="card"><h3>WhatsApp</h3><p className="muted">{c.whatsapp}</p><a className="btn" href={`https://wa.me/${c.whatsapp.replace(/\D/g,'')}`}>OPEN WHATSAPP</a></div></div></div>}
