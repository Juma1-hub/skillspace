"use client";import {useState} from 'react';import {useRouter} from 'next/navigation';import Link from 'next/link';export default function Signup(){const [name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');const router=useRouter();const signup=()=>{if(!name||!email||password.length<4){setMsg('Please complete all fields. Password must be at least 4 characters.');return}localStorage.setItem('ss_logged','true');localStorage.setItem('ss_name',name);localStorage.setItem('ss_email',email);localStorage.setItem('ss_completed','0');localStorage.setItem('ss_unlocked','5');localStorage.setItem('ss_balance','0');window.dispatchEvent(new Event('ss:update'));router.push('/')};return <div className="page"><div className="hero"><h1>Create your SkillSpace account</h1><p>Join a platform connecting clients with people who provide online work.</p></div><div className="form"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Full name"/><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" type="email"/><input value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" type="password"/><button className="primary" onClick={signup}>CREATE ACCOUNT</button><p className="muted">Already have an account? <Link href="/login" style={{color:'#2563eb'}}>Log in</Link></p>{msg&&<div className="notice">{msg}</div>}</div></div>}
