"use client";import {useState} from 'react';import {useRouter} from 'next/navigation';import Link from 'next/link';export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');const router=useRouter();const login=()=>{if(!email||password.length<4){setMsg('Enter your email and a password of at least 4 characters.');return}localStorage.setItem('ss_logged','true');localStorage.setItem('ss_email',email);if(!localStorage.getItem('ss_name'))localStorage.setItem('ss_name',email.split('@')[0]);window.dispatchEvent(new Event('ss:update'));router.push('/')};return <div className="page"><div className="hero"><h1>Welcome to SkillSpace</h1><p>Log in to access work opportunities and connect with clients.</p></div><div className="form"><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/><button className="primary" onClick={login}>LOG IN</button><p className="muted">No account? <Link href="/signup" style={{color:'#2563eb'}}>Create one</Link></p>{msg&&<div className="notice">{msg}</div>}</div></div>}
