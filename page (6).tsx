"use client";import {useEffect,useState} from 'react';export default function Settings(){const [saved,setSaved]=useState(false),[notify,setNotify]=useState(true);useEffect(()=>{setNotify(localStorage.getItem('ss_notify')!=='false')},[]);const save=()=>{localStorage.setItem('ss_notify',String(notify));setSaved(true);setTimeout(()=>setSaved(false),1800)};return <div className="page"><div className="hero"><h1>Settings</h1><p>Control your SkillSpace preferences.</p></div><div className="form"><label><input type="checkbox" checked={notify} onChange={e=>setNotify(e.target.checked)} style={{width:'auto',marginRight:8}}/> Receive task and account notifications</label><br/><button className="primary" onClick={save}>SAVE SETTINGS</button>{saved&&<div className="notice success">Settings saved.</div>}</div></div>}
