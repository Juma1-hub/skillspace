'use client';import {useEffect,useState} from 'react';export default function Profile(){const [name,setName]=useState('Worker'),[email,setEmail]=useState(''),[message,setMessage]=useState('');useEffect(()=>{setName(localStorage.getItem('ss_name')||'Worker');setEmail(localStorage.getItem('ss_email')||'')},[]);const save=()=>{if(!name.trim()){setMessage('Name cannot be empty.');return}localStorage.setItem('ss_name',name.trim());localStorage.setItem('ss_email',email.trim());setMessage('Profile updated successfully.');window.dispatchEvent(new Event('ss:update'))};return <div className="page"><div className="hero"><div className="eyebrow">YOUR ACCOUNT</div><h1>Profile</h1><p>Update the basic information used on your SkillSpace account.</p></div><div className="form"><label>Full name<input value={name} onChange={e=>setName(e.target.value)}/></label><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><button type="button" className="primary" onClick={save}>SAVE PROFILE</button>{message&&<div className="notice success">{message}</div>}</div></div>}
