'use client';
import Link from 'next/link';import {useState} from 'react';
export default function Signup(){const [name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[message,setMessage]=useState('');const submit=(e:React.FormEvent)=>{e.preventDefault();if(!name||!email||password.length<6){setMessage('Enter your name, a valid email and a password of at least 6 characters.');return}localStorage.setItem('ss_name',name);localStorage.setItem('ss_email',email);localStorage.setItem('ss_logged','1');localStorage.setItem('ss_completed','0');localStorage.setItem('ss_unlocked','5');localStorage.setItem('ss_balance','0');localStorage.setItem('ss_done_tasks','[]');localStorage.setItem('ss_transactions','[]');window.location.href='/'};return <div className="auth-page"><div className="auth-card"><div className="logo auth-logo">SKILL<span>SPACE</span></div><h1>Create account</h1><p className="muted">Join SkillSpace and start exploring online work.</p><form onSubmit={submit}><label>Full name<input value={name} onChange={e=>setName(e.target.value)} placeholder="Your name"/></label><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="At least 6 characters"/></label><button className="primary" type="submit">CREATE ACCOUNT</button></form>{message&&<div className="notice warning">{message}</div>}<p className="muted">Already registered? <Link className="text-link" href="/login">Log in</Link></p></div></div>}
