import Link from 'next/link';import {tasks,categories} from '../../lib/data';
export default function Tasks(){return <div className="page"><div className="hero"><h1>Available Tasks</h1><p>Choose a category and open any task to see its full instructions and submit your work.</p></div><div className="grid">{categories.map(c=><div className="card" key={c}><h3>{c}</h3><p className="muted">10 starter/test tasks</p></div>)}</div><div className="grid">{tasks.map(t=><div className="card" key={t.id}><small className="price">{t.category}</small><h3>{t.title}</h3><p className="muted">{t.description}</p><b>${t.reward} USD · KSh {t.reward*130}</b><br/><Link className="more" href={`/tasks/${t.id}`}>VIEW TASK</Link></div>)}</div></div>}
