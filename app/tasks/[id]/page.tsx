'use client';import {useParams} from 'next/navigation';import {tasks} from '../../../lib/data';import {useState} from 'react';
export default function Task(){const {id}=useParams();const t=tasks.find(x=>x.id===id);const [submitted,setSubmitted]=useState(false);if(!t)return <div className="page"><div className="card">Task not found.</div></div>;return <div className="page"><div className="hero"><small className="price">{t.category}</small><h1>{t.title}</h1><p>{t.description}</p><p className="price">Reward: ${t.reward} USD · KSh {t.reward*130}</p>{submitted?<div className="card">Submission received. It is now awaiting review.</div>:<><textarea rows={8} placeholder="Enter your completed work or submission details"/><button className="primary" onClick={()=>setSubmitted(true)}>SUBMIT WORK</button></>}</div></div>}
