'use client';
import Link from 'next/link';import {useState} from 'react';
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[message,setMessage]=useState('');const submit=(e:React.FormEvent)=>{e.preventDefault();if(!email||!password){setMessage('Enter your email and password.');return}localStorage.setItem('ss_logged','1');localStorage.setItem('ss_email',email);window.location.href='/'};return <div className="auth-page"><div className="auth-card"><div className="logo auth-logo">SKILL<span>SPACE</span></div><h1>Welcome back</h1><p className="muted">Log in to access your tasks and earnings.</p><form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/></label><button className="primary" type="submit">LOG IN</button></form>{message&&<div className="notice warning">{message}</div>}<p className="muted">New to SkillSpace? <Link className="text-link" href="/signup">Create an account</Link></p></div></div>}
