'use client';import {useEffect,useState} from 'react';import {defaultContact} from '../../lib/data';export default function Support(){const [contact,setContact]=useState(defaultContact);useEffect(()=>{try{setContact({...defaultContact,...JSON.parse(localStorage.getItem('ss_contact')||'{}')})}catch{}},[]);const whatsapp=contact.whatsapp.replace(/\D/g,'');return <div className="page"><div className="hero"><div className="eyebrow">HELP CENTER</div><h1>Support</h1><p>{contact.message}</p></div><div className="grid"><div className="card"><h3>Email support</h3><p className="muted">{contact.email}</p><a className="btn" href={`mailto:${contact.email}`}>EMAIL SUPPORT</a></div><div className="card"><h3>WhatsApp</h3><p className="muted">{contact.whatsapp}</p><a className="btn" target="_blank" rel="noreferrer" href={`https://wa.me/${whatsapp}`}>OPEN WHATSAPP</a></div></div></div>}
