"use client";import {useEffect,useState} from 'react';export default function Profile(){const [name,setName]=useState(''),[email,setEmail]=useState('');useEffect(()=>{setName(localStorage.getItem('ss_name')||'');setEmail(localStorage.getItem('ss_email')||'')},[]);const save=()=>{localStorage.setItem('ss_name',name||'Worker');localStorage.setItem('ss_email',email);window.dispatchEvent(new Event('ss:update'));alert('Profile saved successfully.')};return <div className="page"><div className="hero"><h1>Profile</h1><p>Manage your worker profile information.</p></div><div className="form"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Full name"/><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" type="email"/><button className="primary" onClick={save}>SAVE PROFILE</button></div></div>}
