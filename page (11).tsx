"use client";import Link from 'next/link';import {useEffect,useState} from 'react';import {money} from '../components/AppShell';export default function Earnings(){const [balance,setBalance]=useState(0),[count,setCount]=useState(0);useEffect(()=>{setBalance(Number(localStorage.getItem('ss_balance')||0));setCount(Number(localStorage.getItem('ss_completed')||0))},[]);return <div className="page"><div className="hero"><h1>Earnings</h1><p>Track rewards from completed tasks and move available funds toward withdrawal.</p></div><div className="grid"><div className="stat"><span className="muted">Available earnings</span><br/><strong>{money(balance)}</strong></div><div className="stat"><span className="muted">Tasks completed</span><br/><strong>{count}</strong></div><div className="stat"><span className="muted">Minimum withdrawal</span><br/><strong>$100 USD</strong><small className="muted"> · KSh 13,000</small></div></div><div className="card"><h2>What next?</h2><p className="muted">Keep completing tasks to build your balance. Withdrawal requests can be submitted from the Withdraw page.</p><Link className="btn" href="/withdraw">Go to Withdraw</Link></div></div>}
